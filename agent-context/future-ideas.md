# Future Ideas

This is not an active roadmap. Use it only as a last resort when current todos are depleted and the user asks for what to do next.

- Define `PaymentPath`, `PayToIdentifier`, `ReceiveCapability`, `PaymentIntent`, `ResolutionRequest`, `AuthorizationScope`, and `ResolverErrorCode`.
- Add OpenAPI generation after the first protocol schema is stable.
- Add mocked resolver and PayToDapp fixtures before publishing integration examples.

